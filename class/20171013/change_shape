void setup(){
  size(200,200);
  background(255);
}

void draw(){
  background(255);
  if(!mousePressed){
    ellipse(width/2,height/2,100,100);
  }else{
    rectMode(CENTER);
    rect(width/2, height/2,100,100);
  }}
