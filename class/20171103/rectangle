void setup() {
  size(600, 600);
  background(255);
}

void draw() {
 rectangle(); //rectangle() roading
}

void rectangle(){ //make rectangle()
    for (int i = 0; i<600; i+=2){
  int c = int(map(i,0,600,0,255));
  fill(0,c,0);
  rect(0,i,600-i,600-i);
}
}
